# 三模式与AST管理复验（2026-09-08）

结论：上次验收提出的4项阻塞问题均通过本轮复验，可通过本轮修正范围的功能验收。此次未修改产品代码，使用临时项目和模拟模型验证功能，不代表真实模型质量或主干收益已验证。

## 原问题逐项核对

| 问题 | 当前行为 | 验证方式 |
| --- | --- | --- |
| 空评分要点仍执行自动初评 | 后端启动前拒绝并列出案例；页面禁用自动初评启动按钮。用户可明确关闭自动初评，仅执行答题 | 后端回归＋浏览器实际操作 |
| AST更新后复用旧会话、缺版本记录 | AST版本变化使旧runtime session失效；每条问答及历史查询保存并返回astVersionId | 实际生成两版AST并用模拟runtime记录session参数的回归 |
| 第三组失败抹除两组有效比较 | 两组独立判断配对资格；前端耗时也按该组对的secondsDelta汇总 | 构造AST失败、其余两组成功的回归＋源码核查 |
| 失败重试重跑全部组并读取新资料 | 新重试批次只包含原失败case/repeat/arm，复制原冻结输入和题库，保留原AST资料版本 | 修改当前题库与源码后重试，检查精确任务集合、冻结源码与版本，并确认原记录不变 |

## 执行记录

- `.venv/bin/python -m unittest discover -s tests`：104项全部通过，包含此前失败的2项HTTP测试。
- `npm test`：8项全部通过。
- `npm run build`：通过，仍有已有的大体积包提示。
- `git diff --check`：通过。

浏览器在加载当前代码的隔离实例验证：

1. 导入仅有问题和参考答案的案例，AST仍为未生成。
2. 在AST管理页点击生成，跳转实验页后显示具体AST版本。
3. 页面提示“有1个案例没有评分要点”，自动初评开启时启动按钮禁用。
4. 取消自动初评后显示“仅答题：预计3次答题”，按钮变为“开始三组答题”。
5. 实际执行结束，页面显示3/3完成、0道有效评分；未把无评分样本算成质量持平。

正常自动评分和HTTP整批执行由本轮回归覆盖；未重新调用真实外部模型，也未改变用户原运行实例或真实项目数据。

## 保留的边界

- 本次结论针对上述4项修正和已执行的功能验证，不是对全部交互的无遗漏保证。
- 三类质量指标仍按评分要点文字关键词归类，并非独立维度评审；题库显式维护类别是后续可优化项，不应将当前展示解释为已经验证的业务质量量表。
- 失败重试采用独立批次，原批次保持原状态；此轮未增加将重试结果自动合并回原批次的功能。
