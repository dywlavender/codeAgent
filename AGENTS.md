# 项目协作指引

## 知识主干与知识图谱整改

涉及项目认知、业务基线、知识主干、词云／知识图谱、入口导航、问答调查或相关页面的设计和修改前，必须先阅读 [知识主干与知识图谱：项目整改指导方向](docs/knowledge-backbone-direction.md)，并以其中用户确认的定义、作用和验收方向为依据。

- 知识主干是供模型建立整体项目认识并持续指导调查的认知地图，包含业务流程、系统职责、术语关系与入口；不能收窄成入口映射。
- 本项目所称词云指具有明确业务语义关系的知识图谱，不能以词频展示或节点数量替代实际作用。
- 整改围绕减少无关探索、正确定位业务范围、减少关键流程遗漏展开；源码确认当前实现，主干提供可修正的调查假设。
- 区分目标、已实现能力和已验证收益；不得把独立图谱浏览或知识文件被读取视为增益证明。
- 当前架构文档描述现状。技术方案可以为上述目标调整，不预设必须恢复旧查询管线或采用固定检索工作流。
- AST 是独立的结构资料，不等同知识主干；只能由用户手动生成并按版本引用。启动项目、进入页面、选择 AST 模式或启动实验不得隐式生成 AST。

若用户明确调整方向，同步更新指导文档和本文件，使后续工作读取到一致的约定。

### 主干收益诊断

按 [诊断验证说明](docs/knowledge-backbone-diagnosis-20260908.md) 区分无主干、仅总览和完整主干，以及代码细节和业务调查题。评分标准与源码冲突时修订标准并独立重评，不给符合源码的答案扣分，不覆盖原批次；不得删减对照组正常资料以制造增益。

AST 对照按 [AST对照方案](docs/ast-comparison-20260908.md) 执行：结构图由冻结源码机械生成；独立AST与总览＋AST分开比较，不能将结构目录或读取次数当作业务图谱收益。

## 多工程边界

- 平台可以登记并切换多个独立工程；工程配置、数据库、工作区、知识维护、题库、AST 和评测批次必须通过 `ProjectContext` 归属到当前工程。
- 规范请求使用 `/api/projects/<projectId>/...`；兼容请求必须明确携带工程上下文，不能用可变的全局当前工程覆盖另一个标签页。
- 工程登记只创建边界和数据目录，不自动同步源码、索引、导入主干或生成 AST；已有历史数据迁移必须显式指定目录或执行迁移步骤。
- 不同工程的会话、资料版本和评测输入不得互相续用；验证 A/B 隔离时要覆盖相同仓库 ID、相同案例 ID 和跨工程会话/版本引用。
