# 2026-08-28

## 熟悉项目 + 架构评审

通读 Business Code Agent（Code Atlas）全项目，产出架构评审结论并写入 MEMORY.md。

## 关键发现：知识生成目前是「半身不遂」

用 sqlite 直查 `.data/validation-project.db` 验证出来的，不是推测：

- **表那一路通了**：5 条 READ/WRITE_TABLE 链接全部落到真实 Mapper 符号
  （`WithdrawalMapper.findApplication`、`SettlementMapper.markSettled` 等）。
- **调用链那一路断了**：6 条 DIRECT_CALL_HINT 的 target 是表达式字符串
  （`'applicationService.create(request.customerId())'`），target_type 是 `CODE_HINT`，
  不是符号 ID，完全不可导航。
- **但数据其实已经够**：`code_fact` 里 14 条 CALL fact 把 Controller→Service→Mapper
  三跳链路完整解析出来了。缺的只是索引层把它接进来 + 把 target 解析成符号。

## 用户决策

明确「先做好知识生成，Query Agent 暂不管」。据此把改造重心放在
`knowledge_update/functional_service.py::_build_index` 与调用目标解析，不动 query_agent。

## 待办（已向用户提出的三步方案，等确认）

1. 调用目标解析器：CALL fact 的 subject（方法名）→ METHOD 符号，唯一则 RESOLVED，多个 AMBIGUOUS
2. `_build_index` 改 BFS：从入口类方法沿调用边走 2-3 跳，产出 CALL_EDGE 链接
3. 补测试（内存库断言「入口能走到 Mapper」）再改实现
